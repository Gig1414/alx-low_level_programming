#include "main.h"
/**
* print _sign - prints the sign of number
* @n the int to check
* Return; 1 and prints + if n is greater than zero 
* 0 and prints 0 if n is zero
* -1 and prints - if n less than zero
*/
int print_ sign(int n)
{
if(n > 0)
{
_putchar('+');
return (1);
} 
else if (n == 0)
}
