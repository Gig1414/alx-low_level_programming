#include "main.h"
/**
* _isalpha - checks for alphabetic character
* @c the character to be checked
* Return 1 if c is letter, 0 otherwise
  */
int is _alphabet(int c)
{
return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'z'));
}
